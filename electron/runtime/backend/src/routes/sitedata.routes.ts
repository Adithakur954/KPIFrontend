// import { Router } from "express";
// import { upload } from "../middleware/multer.middleware.ts";
// import {
//   getSiteData,
//   uploadSiteData,
// } from "../controller/sitedata.controller.ts";

// const siteRouter = Router();

// // ✅ Upload Site Data
// siteRouter.post("/upload-site", upload.single("file"), uploadSiteData);

// // ✅ Get All Site Data (with pagination)
// siteRouter.get("/site-data", getSiteData);

// export { siteRouter };
